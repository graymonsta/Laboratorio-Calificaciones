<UserControl x:Class="UniversityMenuApp.Views.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UniversityMenuApp.Views"
             mc:Ignorable="d" 
             Height="452" Width="559">
    <Grid Margin="24">
        <StackPanel Margin="0,0,-9,0">
            <TextBlock Text="Reportes de Calificaciones" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,179"/>
        </StackPanel>
        <DataGrid HorizontalAlignment="Left"
                  Height="300"
                  Margin="0,54,0,0"
                  VerticalAlignment="Top"
                  Width="490"
                  ItemsSource="{Binding Reportes}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Alumno" Binding="{Binding StudentId}" Width="80"/>
                <DataGridTextColumn Header="Alumno" Binding="{Binding StudentName}" Width="*"/>
                <DataGridTextColumn Header="ID Materia" Binding="{Binding SubjectId}" Width="80"/>
                <DataGridTextColumn Header="Materia" Binding="{Binding SubjectName}" Width="*"/>
                <DataGridTextColumn Header="Nota" Binding="{Binding Nota}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>
        <Label Content="ID" HorizontalAlignment="Left" Margin="0,364,0,0" VerticalAlignment="Top" FontWeight="Bold"/>
        <TextBox Text="{Binding FiltroId, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="20" Margin="25,366,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="50"/>
        <Button Content="Por Alumno" HorizontalAlignment="Left" Margin="85,364,0,0" VerticalAlignment="Top" FontWeight="Bold" Command="{Binding FiltrarPorAlumnoCommand}"/>
        <Button Content="Por Materia" HorizontalAlignment="Left" Margin="175,364,0,0" VerticalAlignment="Top" FontWeight="Bold" Command="{Binding FiltrarPorMateriaCommand}"/>
        <Button Content="Ver Todo" HorizontalAlignment="Left" Margin="265,364,0,0" VerticalAlignment="Top" FontWeight="Bold" Command="{Binding VerTodoCommand}"/>
    </Grid>
</UserControl>
